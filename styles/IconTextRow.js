import { StyleSheet } from "react-native";

export default StyleSheet.create({
    iconText: {
        flexDirection: 'row',
        alignItems: 'center'
    },
    label: {
        color: '#fff',
        fontFamily: 'Helvetica',
        fontSize: 20,
        marginLeft: 10
    }
});
